<% unless environment[:contacts].nil? || environment[:contacts].empty? %>
  <h2>Contacts</h2>
  <ul class="w-100">
    <% environment[:contacts].each do |contact| %>
      <%= render '/contact.*', contact: contact %>
    <% end %>
  </ul>
<% end %>
